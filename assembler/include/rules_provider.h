#if !defined(RULES_PROVIDER_HH)
#define RULES_PROVIDER_HH

#include "lexer.h"

std::map<state_type,rule> get_rules()
{
    std::map<state_type,rule> m;
    m.insert({1,})
}

#endif // RULES_PROVIDER_HH
